

<h1>List of memberships</h1>

<div id="memberships">
    <ul>
        <% @memberships.each do |membership| %>
            <li> <%= render membership %> <%= link_to membership.beer_club.name, membership.beer_club %> </li>
        <% end %>
    </ul>
</div>

<%= link_to "Join a new club", new_membership_path %>