

<h1>Beers</h1>

<%= link_to 'list of breweries', breweries_path %>

<div id="beers">
  <% @beers.each do |beer| %>
    <%= render beer %>
  <% end %>
</div>

<%= link_to('New Beer', new_beer_path) if current_user %>
